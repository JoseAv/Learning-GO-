# Map

Mapa es una estructura de Clave y valor.


``` go
func main(){
    // definimos su clave en [] y el tipo de valor
    m:= make(map[string]int)
    m["apple"] = 2
    m["banana"] = 3
    fmt.Println(m["apple"]) // es 2
}

```