# Slices

Es un array dinamico su funcionamiento es el siguiente.

No definimos tamaño pero sin embargo cuando ah llegado a su limite,este crea otro y copia los anterioros, haciendo asi un array mas grande.


``` go
func main(){
    newSlice:=[]int{1,2,3}
    slice = append(slice,4,5) // agregar elemento
    fmt.Println(slice)

    divideSlice:=[]int{1,2,3,4,5}
    // tomamos desde el indice 1 hasta el 4
    divideSlice := [1:4]
    // Resultado = {2,3,4} no incluye ni el 1 ni el 4


}





```